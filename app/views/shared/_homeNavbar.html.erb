<!-- Navigation -->
<nav class="navbar navbar-fixed-top navbar-inverse" role="navigation">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navigationbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>

          </button>
      <%= link_to raw("<i class='fa fa-user-secret fa-lg'></i> &nbsp; Young Hacks Academy"), root_path, class: "navbar-brand" %>
      
    </div>
        <!-- Collect the nav links, forms, and other content for toggling -->
        <div class="collapse navbar-collapse" id="navigationbar">
            <ul class="nav navbar-nav navbar-right">
                
                <% if current_user %>
                  <li class="<%= yield(:agents) %>"> <%= link_to raw("<i class='fa fa-users fa-lg'></i> Agents"), users_path %></li>
                  <li class="<%= yield(:dashboard) %>"> <%= link_to raw("<i class='fa fa-list-ul fa-lg'></i> DashBoard"), thredded_url %> </li>
                <% end %>
                
                <li>
                  <span>
                    <% if current_user %>
                      <li>
                        <div class="dropdown">
                          <button class="btn btn-defualt dropdown-toggle" type="button" data-toggle="dropdown"><i class="fa fa-user-secret fa-lg">&nbsp;<%= current_user.name %></i> 
                          <span class="caret"></span></button>
                          <ul class="dropdown-menu">
                            <li><%= link_to raw("<i class='fa fa-newspaper-o fa-lg'></i> Show Profile"), current_user %></li>
                            <li><%= link_to raw("<i class='fa fa-pencil-square-o fa-lg'></i> Edit Profile"), edit_user_path(current_user) %></li>
                            <li class="divider"></li>
                            <li><%= link_to raw("</span><i class='fa fa-power-off fa-lg'></i> Sign Out"), signout_path %> </li>
                          </ul>
                        </div>
                      </li>
                    <% else %>
                      <%= link_to raw("<i class='fa fa-google-plus-square fa-lg'></i> Sign In With Google"), "/auth/google_oauth2", id: "sign_in", class: "btn btn-danger"  %>
                    <% end %>
                  </span>
                </li>
                <li><span>&nbsp;</span></li>
            </ul>
        </div>
        <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
</nav>
